#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int n,i,c=1,x=1,sum=0;
    scanf("%d",&n);
    if(n>=0&&n<=10000)
    for(i=1;i<=n;i++)
    {
        sum=sum+(x*(pow(i,3)));
        x=x*-1;
        c++;
    }
    printf("%d",sum);
}
