#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int n,a=0,b=1,c=0;
    scanf("%d\n",&n);
    printf("%d %d ",a,b);
    for(int i=0;i<(n-2);i++)
    {
        c=a+b;
        a=b;
        b=c;
        printf("%d ",c);
    }
}
