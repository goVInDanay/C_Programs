#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int n,a,b,c,temp;
    scanf("%d\n",&n);
    for(int i=0;i<n;i++)
    {
        scanf("%d %d %d\n",&a,&b,&c);
        temp=b/a;
        printf("%d\n",(c*temp));
    }
}
