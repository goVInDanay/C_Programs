#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    float P,R,CI,temp1,temp2;
    scanf("%f",&P);
    scanf("%f",&R);
    temp1=(1+(R/(100.0f)));
    temp2=pow(temp1,2)-1;
    CI=P*temp2;
    printf("%0.2f",CI);
}
