#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int n;
    scanf("%d",&n);
    int d=0;
    while(n!=1)
    {
         d=n%2;
        n=n/2;
       
        
    }
    if(d==0)
        printf("1");
    else
        printf("0");
}
